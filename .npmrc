# inngest declares an OPTIONAL peer on @sveltejs/kit, which npm still tries to
# resolve — and SvelteKit wants a Vite major that conflicts with vitest's.
# We will never use SvelteKit, so the optional peer is noise rather than a real
# constraint. Without this, every `npm i` fails on a dependency we do not have.
legacy-peer-deps=true
